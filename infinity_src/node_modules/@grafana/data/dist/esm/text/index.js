import 'lodash';
import 'marked';
import { escapeHtml, hasAnsiCodes, sanitize, sanitizeTextPanelContent, sanitizeUrl, sanitizeSVGContent } from './sanitize.js';

const textUtil = {
  escapeHtml,
  hasAnsiCodes,
  sanitize,
  sanitizeTextPanelContent,
  sanitizeUrl,
  sanitizeSVGContent
};

export { textUtil };
//# sourceMappingURL=index.js.map
