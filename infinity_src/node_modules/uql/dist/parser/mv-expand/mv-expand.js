"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.mvExpand = void 0;
var lodash_1 = require("lodash");
var utils_1 = require("../utils");
var mvExpand = function (pv, cv) {
    var output = pv.output;
    if (typeof output === "object" && (0, lodash_1.isArray)(output)) {
        output = output
            .filter(function (item) {
            var v = (0, utils_1.get_single_value)(item, cv.value.value);
            return v && (0, lodash_1.isArray)(v) && v.length > 0;
        })
            .flatMap(function (item) {
            var expandingItem = (0, utils_1.get_single_value)(item, cv.value.value);
            if (expandingItem && (0, lodash_1.isArray)(expandingItem)) {
                return expandingItem
                    .map(function (e) {
                    return (0, lodash_1.set)(__assign({}, item), [cv.value.alias || cv.value.value], e);
                })
                    .map(function (item) {
                    if (cv.value.alias) {
                        delete item[cv.value.value];
                    }
                    return item;
                });
            }
            return item;
        });
    }
    return __assign(__assign({}, pv), { output: output });
};
exports.mvExpand = mvExpand;
