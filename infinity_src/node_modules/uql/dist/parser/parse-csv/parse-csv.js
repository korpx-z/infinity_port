"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseCsv = void 0;
var csv_parser = require("csv-parse/lib/sync");
var parseCsv = function (pv, cv) {
    var output = pv.output;
    if (typeof pv.output === "string") {
        var csv_parser_options = get_parse_csv_options(cv.args);
        var result = csv_parser(pv.output, csv_parser_options);
        output = result;
    }
    return __assign(__assign({}, pv), { output: output });
};
exports.parseCsv = parseCsv;
var get_parse_csv_options = function (args) {
    var options = { columns: true, delimiter: ",", skipEmptyLines: false, skipLinesWithError: false, relaxColumnCount: false };
    if (args[0] && args[0].length > 0) {
        args[0].forEach(function (arg) {
            switch (arg.identifier) {
                case "autoParse":
                case "autoParseDate":
                case "bom":
                case "cast":
                case "castDate":
                case "relaxColumnCount":
                case "skipEmptyLines":
                case "skipLinesWithEmptyValues":
                case "trim":
                    options[arg.identifier] = arg.value.toLowerCase() === "true";
                    break;
                case "columns":
                    options[arg.identifier] = arg.value ? arg.value.split(",") : true;
                    break;
                default:
                    // @ts-ignore
                    options[arg.identifier] = arg.value.toLowerCase() === "true" ? true : arg.value.toLowerCase() === "false" ? false : arg.value;
                    break;
            }
        });
    }
    return options;
};
