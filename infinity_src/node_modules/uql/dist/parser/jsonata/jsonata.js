"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.jsonata = void 0;
var lodash_1 = require("lodash");
var JSONata = require("jsonata");
var jsonata = function (pv, cv) {
    var expression = JSONata(cv.expression);
    var out = expression.evaluate(pv.output);
    if (out && typeof out === "object" && (0, lodash_1.isArray)(out)) {
        delete out.sequence; // https://github.com/jsonata-js/jsonata/issues/296
        delete out.keepSingleton; // https://github.com/jsonata-js/jsonata/issues/469
    }
    return __assign(__assign({}, pv), { output: out });
};
exports.jsonata = jsonata;
